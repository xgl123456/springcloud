#eureka:
#  client:
#    serviceUrl:
#      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: product-view-service-feign  
#  zipkin:
#    base-url: http://localhost:9411  
#  thymeleaf:
#    cache: false
#    prefix: classpath:/templates/
#    suffix: .html
#    encoding: UTF-8
#    mode: HTML5 #    servlet:#      content-type: text/html
management:  endpoints:    web:      exposure:        include:        - "*"
      cors:        allowed-origins:        - "*"  
        allowed-methods:        - "*"
feign:
  hystrix:
   enabled: true
hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 6000 # 设置hystrix的超时时间为6000ms,解决一直走fallback